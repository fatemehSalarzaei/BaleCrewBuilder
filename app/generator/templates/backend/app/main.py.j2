from fastapi import FastAPI

from app.api.router import api_router

app = FastAPI(
    title="{{ project.name }}",
    version="0.1.0",
)

app.include_router(api_router)


@app.get("/health", tags=["health"])
async def health_check() -> dict:
    return {"status": "ok", "project": "{{ project.slug }}"}
