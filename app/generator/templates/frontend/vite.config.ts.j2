import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";

// Frontend for {{ project.name }} ({{ project.slug }})
export default defineConfig({
  plugins: [react()],
  server: {
    port: 3000,
    proxy: {
      "/api": {
        target: "http://localhost:8000",
        changeOrigin: true,
      },
      "/auth": {
        target: "http://localhost:8000",
        changeOrigin: true,
      },
    },
  },
});
